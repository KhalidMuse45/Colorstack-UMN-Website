{
  "name": "colorstack-umn-web",
  "private": true,
  "version": "0.0.1",
  "_comment": [
    "Dependency decisions, recorded so the next pass does not relitigate them.",
    "",
    "React 19, not the handoff's 18.3. Next 15 App Router expects React 19 and",
    "@react-three/fiber v8 does not support React 19, so the pinned pair in",
    "handoff/next/package.json cannot install together. Moved to the supported",
    "combination: react 19 + @react-three/fiber 9 + @react-three/drei 10.",
    "",
    "The Sanity group (sanity, next-sanity, @sanity/*, @portabletext/react) is",
    "deliberately absent. There is no Sanity project yet, so nothing would be",
    "fetched and /studio would be an empty route. lib/landing.ts holds the swap",
    "point and the same return shape.",
    "",
    "framer-motion is deliberately absent. Its only sanctioned use is CardDeck",
    "(docs/03 performance rules), which is not in this pass.",
    "",
    "@eslint/eslintrc is plumbing: eslint 9 flat config loading the eslintrc-",
    "format eslint-config-next presets through FlatCompat."
  ],
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint . && sh scripts/guardrails.sh"
  },
  "dependencies": {
    "@gsap/react": "^2.1.2",
    "@react-three/drei": "^10.7.6",
    "@react-three/fiber": "^9.3.0",
    "gsap": "^3.13.0",
    "lenis": "^1.3.11",
    "next": "^15.5.4",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.1",
    "@types/node": "^22.0.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@types/three": "^0.180.0",
    "eslint": "^9.36.0",
    "eslint-config-next": "^15.5.4",
    "typescript": "^5.9.0"
  }
}
